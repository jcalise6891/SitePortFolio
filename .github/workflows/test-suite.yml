name: TestSuite
on:
  pull_request:
    branches:
      - master

jobs:

  phpcs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Validate composer
        run: composer validate


